server: # 端口
  port: 8090

spring:
  datasource: # 数据源
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456Zxc.
    url: jdbc:mysql://119.91.208.187:3306/law_app?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false


mybatis-plus:
  configuration: # mybatis-plus 配置
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl 
  global-config:  # mybatis-plus 全局配置
    db-config:
      # table-prefix: t_ # 表前缀
     logic-delete-value: 1 # 逻辑已删除值
     logic-not-delete-value: 0 # 逻辑未删除值